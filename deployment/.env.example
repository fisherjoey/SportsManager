# ===========================================
# Sports Manager - Environment Configuration
# ===========================================
# Copy this file to .env and customize for your environment
# NEVER commit .env files with real passwords to Git!

# ===========================================
# Database Configuration
# ===========================================
DB_HOST=postgres
DB_PORT=5432
DB_NAME=sports_management
DB_USER=postgres
# IMPORTANT: Change this password in production!
DB_PASSWORD=postgres123
DATABASE_URL=postgresql://postgres:postgres123@postgres:5432/sports_management

# ===========================================
# JWT Configuration
# ===========================================
# CRITICAL: Generate a strong secret for production!
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=local-dev-secret-key-change-in-production
JWT_EXPIRES_IN=7d

# ===========================================
# Application Configuration
# ===========================================
NODE_ENV=development
PORT=3001

# ===========================================
# Frontend Configuration
# ===========================================
NEXT_PUBLIC_API_URL=http://localhost:3001/api
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001/api
NEXT_PUBLIC_ENV=development
FRONTEND_URL=http://localhost:3000

# ===========================================
# Cerbos Authorization Service
# ===========================================
CERBOS_HOST=cerbos:3593
CERBOS_TLS=false
CERBOS_CACHE_ENABLED=true
CERBOS_CACHE_TTL=300000

# ===========================================
# Redis Configuration
# ===========================================
# Set to true to disable Redis and use in-memory queue
DISABLE_REDIS=true
# REDIS_URL=redis://redis:6379

# ===========================================
# CORS Configuration
# ===========================================
# Comma-separated list of allowed origins
PRODUCTION_DOMAINS=http://localhost:3000,http://localhost:3004

# ===========================================
# Rate Limiting
# ===========================================
# More permissive for development
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=10000

# ===========================================
# Location Services
# ===========================================
# Options: google, mapbox, openroute
LOCATION_SERVICE=openroute
OPENROUTE_API_KEY=your-api-key-here

# Optional alternative providers:
# GOOGLE_MAPS_API_KEY=your-google-api-key
# MAPBOX_ACCESS_TOKEN=your-mapbox-token

# ===========================================
# Email Configuration (Optional)
# ===========================================
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# EMAIL_FROM=noreply@yourapp.com

# ===========================================
# Monitoring (Optional)
# ===========================================
# SENTRY_DSN=https://your-sentry-dsn
# NEXT_PUBLIC_SENTRY_DSN=https://your-sentry-dsn

# ===========================================
# Feature Flags
# ===========================================
NEXT_PUBLIC_ENABLE_DEBUG=true
NEXT_TELEMETRY_DISABLED=1

# ===========================================
# Production Security Checklist
# ===========================================
# Before deploying to production:
# [ ] Change DB_PASSWORD to a strong password (16+ characters)
# [ ] Generate new JWT_SECRET using crypto.randomBytes
# [ ] Replace all API keys with production keys
# [ ] Set NODE_ENV=production
# [ ] Configure proper FRONTEND_URL and PRODUCTION_DOMAINS
# [ ] Enable SSL/TLS (CERBOS_TLS=true)
# [ ] Reduce RATE_LIMIT_MAX_REQUESTS to reasonable limit (e.g., 100)
# [ ] Enable monitoring (Sentry, etc.)
# [ ] Set up email service for notifications
# [ ] Review and test all configurations
