# Development Dockerfile for Frontend with Hot-Reload
FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies (use npm install for dev, not ci)
RUN npm install

# Copy source code
COPY . .

# Expose port
EXPOSE 3000

# Start Next.js in development mode (hot-reload built-in)
CMD ["npm", "run", "dev"]
