---
apiVersion: api.cerbos.dev/v1
description: Policy for invitation resource
resourcePolicy:
  version: "default"
  importDerivedRoles:
    - common_roles
  resource: "invitation"
  rules:
    - actions: ['create', 'view:list', 'delete']
      effect: EFFECT_ALLOW
      roles:
        - admin
      derivedRoles:
        - organization_admin

    - actions: ['*']
      effect: EFFECT_DENY
      roles:
        - assignor
        - referee
        - guest